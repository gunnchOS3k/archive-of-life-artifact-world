# Pipeline exports

Generated bundles, audit outputs, and scoped extracts land here before promotion to `public/data/`.

- `sample_scope/` — small scoped JSON used by audits (committed)
- Other large exports are gitignored unless explicitly tracked

Run `npm run pipeline:build-snapshot` to regenerate export manifests.
