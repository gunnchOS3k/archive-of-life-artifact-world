# WIP protection notes

Before Gate 1 branching, dirty status JSON under `public/data/status/` and `.vite/` were stashed:

`stash@{0}: WIP-protect-before-gate1-workstream-e 20260806T200108Z` on `feat/final-completion-pass`

Restore with: `git checkout feat/final-completion-pass && git stash pop` (when ready).
Patch copy also under `repos/_wip_protect/archive-of-life-*`.
